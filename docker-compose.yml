version: "3.8"
services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    ports: ["4000:4000"]
    env_file: .env
  pdf-processor:
    build: ./services/pdf-processor
    ports: ["8000:8000"]
    env_file: .env
  redis:
    image: redis:alpine
    ports: ["6379:6379"]
